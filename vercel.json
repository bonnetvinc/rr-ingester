{
  "version": 3,
  "buildCommand": "pnpm --filter @repo/database db:migrate-deploy && pnpm --filter @repo/database db:generate && pnpm --filter raw-injester build",
  "installCommand": "pnpm install",
  "outputDirectory": "apps/raw-injester/dist",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "apps/raw-injester/dist/api/index.js"
    }
  ]
}
